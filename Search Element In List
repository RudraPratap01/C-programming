#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int n,found=0;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int value;
    scanf("%d",&value);
        for(int i=0;i<n;i++){
            if(value==arr[i]){
                found=1;
                }
        }
    if(found==1){
        printf("Found");
    }
    else{
        printf("Not Found");
    }
    return 0;
}
